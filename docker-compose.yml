version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: bqg
      POSTGRES_PASSWORD: bqgpassword
      POSTGRES_DB: nestdb
    ports:
      - 5499:5432